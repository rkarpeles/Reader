edButtons[edButtons.length] =
new edButton('ed_h1'
	,'h1'
	,'<h1>'
	,'</h1>'
	,'1'
);

edButtons[edButtons.length] =
new edButton('ed_h2'
	,'h2'
	,'<h2>'
	,'</h2>'
	,'2'
);

edButtons[edButtons.length] =
new edButton('ed_h3'
	,'h3'
	,'<h3>'
	,'</h3>'
	,'3'
);

edButtons[edButtons.length] =
new edButton('ed_h4'
	,'h4'
	,'<h4>'
	,'</h4>'
	,'4'
);

edButtons[edButtons.length] =
new edButton('ed_h5'
	,'h5'
	,'<h5>'
	,'</h5>'
	,'5'
);

edButtons[edButtons.length] =
new edButton('ed_q'
	,'q'
	,'<q>'
	,'</q>'
	,'6'
);

edButtons[edButtons.length] =
new edButton('ed_cite'
	,'cite'
	,'<cite>'
	,'</cite>'
	,'6'
);

edButtons[edButtons.length] =
new edButton('ed_sub'
	,'sub'
	,'<sub>'
	,'</sub>'
	,'7'
);

edButtons[edButtons.length] =
new edButton('ed_sup'
	,'sup'
	,'<sup>'
	,'</sup>'
	,'8'
);

edButtons[edButtons.length] =
new edButton('ed_dl'
	,'dl'
	,'<dl>'
	,'</dl>'
	,'9'
);

edButtons[edButtons.length] =
new edButton('ed_dt'
	,'dt'
	,'<dt>'
	,'</dt>'
	,'10'
);

edButtons[edButtons.length] =
new edButton('ed_dd'
	,'dd'
	,'<dd>'
	,'</dd>'
	,'11'
);